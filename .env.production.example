# ShareLocal Production Environment Variables
# Kopiere diese Datei zu .env.production oder .env.dev und fülle alle Werte aus

# ============================================
# Database Configuration
# ============================================
POSTGRES_USER=sharelocal
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=sharelocal

# ============================================
# API Configuration
# ============================================
NODE_ENV=production
PORT=3001
LOG_LEVEL=info

# Database Connection String
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://sharelocal:change-me-in-production@postgres:5432/sharelocal?schema=public

# JWT Secret (min. 32 Zeichen)
# Generiere mit: openssl rand -base64 32
JWT_SECRET=change-me-in-production-min-32-chars-required

# Encryption Key für Chat-Nachrichten (min. 32 Zeichen)
# Generiere mit: openssl rand -base64 32
ENCRYPTION_KEY=change-me-in-production-min-32-chars-required

# ============================================
# Web Configuration (Next.js)
# ============================================
# API URL für Frontend (mit HTTPS in Production)
NEXT_PUBLIC_API_URL=https://nuernbergspots.de/share-local/prd/api

# Base Path für Sub-Path Deployment
# Für Production: /share-local/prd
# Für Development: /share-local/dev
NEXT_PUBLIC_BASE_PATH=/share-local/prd

# ============================================
# Notes
# ============================================
# 1. Generiere sichere Secrets:
#    openssl rand -base64 32  # Für JWT_SECRET
#    openssl rand -base64 32  # Für ENCRYPTION_KEY
#    openssl rand -base64 32  # Für POSTGRES_PASSWORD
#
# 2. Für Development Environment:
#    - Setze NODE_ENV=development
#    - Setze LOG_LEVEL=debug
#    - Setze NEXT_PUBLIC_API_URL=http://nuernbergspots.de/share-local/dev/api
#    - Setze NEXT_PUBLIC_BASE_PATH=/share-local/dev
#    - Optional: Verwende separate Database (POSTGRES_DB=sharelocal_dev)
#
# 3. WICHTIG: Ändere alle "change-me-in-production" Werte!
#    Die API wird nicht starten, wenn Secrets nicht geändert wurden.
