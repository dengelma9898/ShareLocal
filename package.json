{
  "name": "sharelocal",
  "version": "0.1.0",
  "private": true,
  "description": "Digitale Vermittlungsplattform für Ressourcen-Sharing in lokalen Gemeinschaften",
  "license": "AGPL-3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "api:dev": "pnpm --filter @sharelocal/api dev",
    "web:dev": "pnpm --filter @sharelocal/web dev",
    "mobile:dev": "pnpm --filter @sharelocal/mobile dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
