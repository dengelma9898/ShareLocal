# Docker Compose für Production Environment
# Verwendung: docker-compose -f docker-compose.yml -f docker-compose.prd.yml up

version: '3.8'

services:
  api:
    environment:
      NODE_ENV: production
      LOG_LEVEL: info

  web:
    build:
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://nuernbergspots.de/share-local/prd/api}
        NEXT_PUBLIC_BASE_PATH: /share-local/prd
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://nuernbergspots.de/share-local/prd/api}
      NEXT_PUBLIC_BASE_PATH: /share-local/prd

